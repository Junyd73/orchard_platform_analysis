# 04. Mobile screen — 판매관리

> 단계 2 **완료/대표 승인**. **판매관리 4탭 Shell**. Stage 6 판매/출고 UX **1차 구현** (`/orders/ship`). Stage 3B 배정 UI **후순위**.  
> UX 원칙: [09 §0](./09_production_inventory_flow.md) · ODS · [SCR-030](../../mobile/docs/screens/SCR-030.md)

## 0. UX (DEC-021)

- 같은 정보 **두 번 입력하지 않음** (생산수량 → 판매 재입력 금지, A안)
- 배정·출고는 **선택 경로** — 미배정을 오류/경고로 강조하지 않음
- 날짜·채번 자동 · 필드 최소화 · Batch 번호 사용자 입력 금지

---

## 현재 코드 (단계 2 + 판매관리 Shell)

- 하단 5탭: 홈 · 생육관찰 · 영농일지 · 농약관리 · **판매관리**
- `/orders` → 상단 **[포장/생산] [재고] [주문] [판매]** (4등분 segmented)
- 초기 탭: **주문** (목록·등록·상세 기존 Stage 2)
- [포장/생산]: **Stage P** — `PackProdPanel` (배 포장 / 배즙 PROCESS·일반배즙|도라지배즙 선택 · 수확직후/저장원물 · 생산확정 · 재고저장/바로판매 prefill)
- [재고]: Stage **5B** — `StockView`. 수기 IN/OUT **금지**. 카드 **판매** → `/orders/ship`
- [판매]: 목록 placeholder. 확정 후 `?tab=sales` 복귀. 공통 화면은 `/orders/ship`
- `/orders/new` 신규 주문. `/orders/:orderNo` 상세(조회·출고 CTA)
- `/orders/ship` 공통 판매/출고 (`ShipConfirmView`)
- AppBar 톱니 → `/settings` (세션·농장·내정보 셸)
- 목록에 가짜 배정/이행 배지 없음 (단계 3 전)

**4탭은 업무영역 분류.** 포장→재고→주문→판매 **강제 순서 아님** (DEC-021).

---

## 1. 하단 메뉴 (DEC-006 APPROVED)

| # | 탭 | path |
|---|-----|------|
| 1 | 홈 | `/` |
| 2 | 생육관찰 | `/observation` |
| 3 | 영농일지 | `/work-log` |
| 4 | 농약관리 | `/pesticide` |
| 5 | **판매관리** | `/orders` |

내정보: 하단에서 제거. 우측 상단 톱니 → 환경설정 → 내정보.  
환경설정 1차는 셸+세션/농장만. 전면 설정 앱은 비범위.

아이콘: `nav-orders.svg` 재사용 검토. `mainTabNav.ts` 스와이프 SSOT 동기화.

---

## 2. 진입 정보 구조

```
OdsAppBar  (농장명)              [톱니]
OdsSegmented  [포장/생산] [재고] [주문] [판매]   ← 초기: 주문
────────────────────────────────
(선택된 탭 — 주문: 실목록 / 포장·생산: PackProdPanel / 재고: StockView / 판매: placeholder)
FAB  주문탭: + 신규 주문 / 판매탭: + 직접 판매 (placeholder toast)
OdsBottomNav  … 판매관리
```

가락 DRAFT는 시세 연동 후 판매 목록에 나타남 (단계 6).

---

## 3. 주문 목록

필터: 검색(고객·번호) · **주문상태** (ST01: 예약접수~취소, DEC-011 CLOSED) · 기간.  
이행상태 필터는 계산값 (미배정/부분배정/배정완료/출고완료). ST01과 한 콤보로 섞지 않음.

카드 필드:

| 표시 | 소스 |
|------|------|
| 고객 | `custm_nm` |
| 주문일 | `order_dt` (표시 KST 날짜, 파싱 혼재 허용) |
| 출고예정일 | `MIN(planned_dt)` |
| 주문상태 | `status_cd` 명칭 (매핑 전 코드 원문 가능) |
| 재고상태(이행) | **단계 3 전 표시하지 않음** (허위 배정상태 금지) |
| 주문수량 | `SUM(qty)` |
| 주문금액 | `tot_order_amt` |
| 선입금 | `pre_pay_amt` (전표 아님) |

행 탭 → 상세. FAB → 등록.

---

## 4. 주문 등록/수정

- 고객 (이름·연락처 필수)
- 주문일 기본 `today_ops` → 저장 `YYYY-MM-DD`
- 출고예정일 (`planned_dt`)
- 시즌 `SS01`
- 상품 N행: 품종 · 중량 · 등급 · 크기/과수 · 수량 · 단가 · 금액
- 배송: 방문 / 택배 / (필요 시 화물)
- 방문수령 시 주소 비필수
- 선입금(금액만) · 메모

**검증:** 고객, 상세 ≥1, 행 수량 = 배송계획 합.  
**재고 0이어도 저장.** 가용 부족은 경고만.  
저장 시 판매 행 없음, `allocated_qty=0`.

수정: `stock_status='Y'` 이면 거부.

---

## 5. 주문 상세

헤더: 번호 · 고객 · **주문상태** · **재고상태(이행)** · 시즌 · 금액 · 선입금

규격별 줄 예:

```
주문 100    배정 30    미배정 70    출고 0
```

내부 계약: 「배정」= `allocated_qty`(누적). 「출고」= `shipped_qty`. 사용자에게 누적이라는 말을 쓰지 않음.  
미배정은 오류가 아니다. 안내: 「저장재고 출고 시 재고를 배정합니다.」 (DEC-020). 「미배정 → 반드시 처리」로 표시하지 않음.

배송 목록. 입금: 주문 선입금 + (판매 후) 수금 합.

출고 CTA: 배정잔량 있으면 기본 「배정재고 사용」(STOCK), 없으면 「일반재고에서 출고」(DIRECT). 개발 용어 STOCK/DIRECT를 화면에 쓰지 않음. 상세는 [§9](#9-stage-6-판매출고-화면-시나리오).

액션: 배정 · 배정해제(`release <= 배정-출고`) · 출고확정 · 취소(출고 전만)

부분출고 후 판매 탭에 판매가 여러 장 보인다 (DEC-017: 출고 1회 = 판매 1건). 연결 조회는 `t_sales_master.order_no`. 주문상세 잔여 표시 SSOT는 confirm 응답 `remaining_order[]`.

주문상태와 재고상태를 한 배지로 합치지 않음. ODS `OdsCard` 메타 두 줄 또는 칩 두 개.

---

## 6. 판매 탭 (1차: 화면 구조)

| 필터 | 조건 |
|------|------|
| 가락 초안 | `AUCTION_RT` + `DRAFT` |
| 확정 판매 | `CONFIRMED` |
| 소매 | `order_no` 있음 |
| 수출/도매 | 주문 없음 · source ORDER |

카드: 판매번호 · 일자 · 고객/법인 · 금액 · 미수 · DRAFT 배지.

상세: 규격 줄 · 배송 · 수금(CONFIRMED만) · DRAFT면 **판매확정** CTA (PC에도 동일 서비스).

직접 판매: 고객 · 규격 · 수량 · 단가 · 배송 · 출고 포함.

한 주문에서 부분출고가 여러 번이면 판매 탭에 **판매 N건**이 같은 `order_no`로 나타난다. `t_order_master.sales_no`로 목록을 만들지 않는다.

---

## 7. ODS 참고

- 목록+FAB: SCR-001 / SCR-020
- 문서 리스트: 농약 입고 목록
- 폼: `mobile/docs/ODS/MOBILE_FORM_READABILITY.md`
- 세그먼트: `OdsSegmented`

시각 픽셀은 본 문서에서 고정하지 않는다.

---

## 8. 비범위 (화면)

- PC 주문 레이아웃 이식, 엑셀 배송 템플릿
- 재고 실사/원물 입고 모바일 화면
- 환경설정 전면 앱 (1차는 세션·농장·내정보 셸만)
- Stage 6 1차: 같은 화면 **연속 출고**, 로트(`stock_seq`) 수동 선택, STOCK/DIRECT 개발용어 노출
- 판매취소·반품·가락 DRAFT 확정 UI (후속)

---

## 9. Stage 6 판매/출고 화면 시나리오

**구현 (1차):** 화면 `ShipConfirmView` · 경로 `/orders/ship` · `POST /farms/{farm_cd}/shipments/confirm`. Vue **착수 완료**.  
**API:** `POST /api/v1/farms/{farm_cd}/shipments/confirm` 만. FIFO·allocation·reserved는 Core.

### 9.1 한 화면, 세 진입

```
생산 직후 ─┐
주문 상세 ─┼→ 판매/출고 화면 → confirm API
재고 화면 ─┘
```

판매 화면을 진입점마다 나누지 않는다. 차이는 **prefill** 뿐.

화면에 보일 것: 고객 · 품목(규격) · 수량 · 단가 · 주문 연결 여부 · 출고방식(농부 문구).  
숨길 것: `stock_seq`, FIFO 분할, allocation consume, reserved.

확정 성공 후 잔여 **화면 SSOT** = `remaining_order[]` (줄 단위).  
`remaining_order_qty`는 합 요약만. 완료 여부는 `order_status` (`ST010300`/`ST010400`). 무주문이면 둘 다 비움.

### 9.2 정책 4항

| # | 정책 | 내용 |
|---|------|------|
| 1 | 생산 직후 바로 판매 | 기본 **DIRECT**. 생산 IN 완료 후 prefill. **무주문 강제 아님**(설계). **1차:** 대형 주문검색 UI 없음 → 생산 진입은 무주문 DIRECT |
| 2 | 주문에서 출고 | 배정잔량 있으면 기본 「배정재고 사용」= STOCK. 없으면 「일반재고에서 출고」= DIRECT. 사용자가 변경 가능. 화면 라벨에 STOCK/DIRECT 금지 |
| 3 | 재고 탭 → 판매 | 카드 1장 또는 **선택 n건**. 규격 prefill. `stock_seq` 강제 금지. Core FIFO |
| 4 | 주문 일괄/부분 출고 | 상세에서 상품별 출고 · 선택 출고 · 잔여 일괄출고. 한 confirm = 한 판매 |

### 9.3 진입별 prefill

**A. 생산 직후**

```
생산확정 TX (전량 IN)
→ [바로 판매]
→ salesPrefill (규격·수량 초안, 전량 판매 강제 없음)
→ 판매/출고 화면, 출고방식 기본 DIRECT
→ 1차: 주문 연결 UI 없음 (무주문 DIRECT)
→ confirm
```

생산 화면에서 80/20 분할 입력 금지 (DEC-023). 판매 수량만 줄이면 잔량은 상품재고로 남음.

**B. 주문 상세**

```
주문 10, 배정잔량 6 → 기본 배정재고(STOCK), 출고수량 기본은 잔량 이하
배정 0 → 기본 일반재고(DIRECT)
→ confirm
→ 주문상세: remaining_order[] 로 「출고 n / 잔여 m」
```

무주문+STOCK은 Core가 거부. 화면이 그 조합을 만들지 않음.

**C. 재고 탭**

카드 [판매] 또는 선택 후 [선택 n건 판매]. 배즙 출고 줄 표시는 제품명(일반배즙/도라지배즙)만. 이력 시트에서 증가/감소(사유 코드, 전표 없음).

```
상품 재고카드 탭
→ 규격만 판매화면으로
→ 주문 없음 + DIRECT
→ 수량 ≤ 가용(표시는 재고 조회 available, 확정은 Core 재검증)
```

선택한 저장일/stock_seq를 payload에 넣지 않음.

### 9.4 확정 후 동선 (1차)

성공: 짧은 완료 안내(`sales_no`) 후 **같은 폼에서 이어서 출고하지 않음**.

- 주문 상세 진입 → 해당 주문 상세 복귀(재조회)
- 재고 진입 → `/orders?tab=stock` (재조회)
- 생산 진입 → `/orders?tab=sales`

부분잔량은 주문상세 **출고** CTA로만 재진입.

실패: confirm 오류 메시지. 재고/화면 수량은 Core 거절 그대로. 로컬 재계산으로 덮지 않음.

### 9.5 구현 시 금지

- Client `stock_seq` 지정
- `remaining_order_qty`만으로 줄 완료 판정
- 판매화면 3벌 복제
- 출고 API를 주문 path로 다시 만들기
- Stage 6에서 판매취소·반품·가락 DRAFT 확정


# SCR-004 생육관찰 상세조회

- **상태:** Approved (ODS v1.0.1) + Orchard Design System (ODS) v1.1 적용
- **원본:** `mobile/docs/ODS/ODS_v1.0.pdf`, `ODS_v1.0.1_SCR-004_Addendum.md`
- **공통 정책:** `mobile/docs/ODS/ODS_v1.md` (ODS v1.1)
- **참고 시안:** `mobile/docs/ODS/assets/SCR-004_approved_reference.png`
- **구현:** `mobile/src/features/observation/ObservationDetailView.vue`

## 카드 구조

1. 공통 AppBar (`OdsAppBar`) — 농장명 / 알림 / 설정 + **화면 Back**
2. Hero — 유형·제목·Badge·위치칩·메타·일러스트 (화면 실제 제목)
3. 관찰 내용 (`OdsCard`)
4. 사진 (`PhotoPanel` variant `scr004`, `OdsCard`)
5. AI 분석 (`OdsCard`)
6. 스마트 방제 가이드 (`OdsCard`)
7. 관리정보 (`OdsCard` Accordion, 기본 접힘)
8. 하단 수정·삭제 + `OdsBottomNav`

## 화면 규칙 (SCR-004 전용)

ODS 공통 버전은 올리지 않는다. 아래는 본 화면 보완 규칙이다.

### 등록 플로우 연계 (수정 진입)

상세에서 수정으로 들어갈 때 등록 Wizard(SCR-002) 규칙을 따른다.

- Step는 진행 상태 표현. 현재 단계는 Amber(`--ods-color-accent` / `--ods-color-caution`)
- 관찰 대상은 필수 선택. 신규 초기 기본 선택 없음
- 다음 버튼은 관찰 대상 선택 후 활성

### 사진 가로 스와이프 (Horizontal Media Strip)

- 최대 등록 수 **5장** 유지. 데이터는 전부 DOM에 렌더한다(처음 3장만 그리지 않음).
- 가로 스크롤 + `scroll-snap`. 일반 모바일에서 약 **2.7~3장** 노출(다음 장 일부 보임).
- 빈 슬롯 카드·강제 중앙 정렬 금지.
- 간격은 ODS spacing token. 스크롤바는 숨김.
- 클릭 → 기존 PhotoViewer. 삭제·대표·순서 로직 변경 없음.
- 수량 표시: `n / 5 장` (기존 count).

### 사진 추가 버튼

| 이전 | 변경 |
|------|------|
| 카메라 촬영 | 카메라 |
| 갤러리 선택 | 갤러리 |

- 시각적 높이·패딩·텍스트를 한 단계 축소(보조 액션). 터치 영역 ≥ 44×44px.
- 아이콘 유지. 5장 가득 시 기존 비활성화 정책 유지.

## 금지

- 별도 사진관리 버튼
- Emoji
- AppBar에 화면명 / 햄버거 (본 화면 Back은 위 규칙 예외)
- 승인 시안 외 임의 디자인
- 사진 최대 수를 3으로 축소 / 외부 캐러셀 라이브러리 추가
